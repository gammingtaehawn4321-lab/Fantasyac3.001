# 판타지악 3.2 패치

## 적용 항목
1. 퀘스트/모험/AI 상태변경 보상으로 획득한 가방의 메타데이터 보존 및 가방 미장착 상태에서 첫 가방 자동 장착.
2. 아이템 설명/사용처 fallback 보강, QUEST/MAP 분류 및 지도 실제 사용 처리.
3. 일반 인카운터 발생 Hex 귀속 및 인카운터 내부 인접 Hex 이동을 실제 월드맵 좌표에 반영. 기존 목적지 TravelSession과 분리.
4. Fate 완료 상태 정규화 및 퀘스트 단계 조건 재평가 보강.
5. 플로팅 모험 버튼이 로그 마지막 부분을 가리지 않도록 로그 내용 하단 안전 여백 적용.
6. 상태/가방/퀘스트 등 차단형 UI가 열렸을 때 플로팅 모험 버튼을 숨겨 겹침 방지.
7. 사용자가 체형 탭에서 저장한 키/체격/가슴/엉덩이 값을 종족 전형보다 우선 사용.
8. 퀘스트 알림을 단순 진행 중 퀘스트 수와 분리. 새 제안/목표 갱신/완료/후속 퀘스트 등 미확인 변화에만 표시하고 퀘스트 창 확인 시 해제.
9. 모험 플로팅 버튼 위치를 오른쪽 하단 기준으로 유지.
10. 전직/액티브/패시브/장비/퀘스트 보상/생활직업 등을 정적 전수 점검하고 발견된 연결 누락 수정.
11. 세이브/로드 슬롯 위치를 시작 지역 고정값이 아니라 실제 저장 당시 월드/던전 위치에서 계산.
12. Gemini가 정상 게임 로그 대신 거부/무효 응답을 반환하면 동일 요청을 제한 횟수 자동 재시도. 성공 응답 하나만 클라이언트 상태에 반영.

## 3.2 전수 점검 중 추가 수정
- 장비에서 참조하지만 스킬 DB에 없던 `cleric_purification`, `mage_starlight_singularity` 복구.
- `heavy_stagger_boost` 장비 특성이 실제 경직 피해 계산에 적용되도록 연결.
- QuestRewards에 정의되어 있으나 실제 지급되지 않던 장비/열쇠/생활직업 경험치/레시피/동료 신뢰/세력 평판/지역 해금 보상 연결.
- 생활직업 경험치 대량 획득 시 한 번만 레벨업하던 문제를 다중 레벨업 처리로 수정.
- 캐릭터 종족/고유 패시브 ID를 전투 Actor trait에 노출하여 런타임 효과 시스템에서 참조 가능하게 보강.
- 지역 해금 보상이 실제 Hex ID일 때만 discoveredHexIds에 들어가도록 데이터 오염 방지.

## 검증
- TypeScript 정적 검사에서 외부 패키지 미설치에 따른 모듈 타입 오류를 제외한 내부 진단 0건.
- 클래스/성장 스킬 참조 누락 0건.
- 장비 마법 스킬 참조 누락 0건.
- 장비 specialEffectIds 중 단독 정의되어 실제 사용처가 없는 리터럴 0건.
- Gemini 기존 모델 폴백(`gemini-3.6-flash` → `gemini-3.7-flash` → `gemini-flash-latest`) 유지 확인.

## 3.2 오류 박멸 이어서 점검 추가 수정
- 한 이벤트가 퀘스트 단계 전환 뒤 다음 단계까지 중복 소비되던 문제 수정. 상태 기반 목표만 단계 전환 후 재평가.
- 퀘스트 보상 EXP에 일반 레벨업 공식을 연결해 다중 레벨업/스탯/재능/HP·MP 성장/마일스톤 정상 적용.
- 퀘스트 보상 아이템·장비·열쇠/생활직업 레벨업/동료 신뢰를 중앙 GameEvent에 연결.
- 잠금 해제 이벤트를 보상 지급보다 먼저 처리하도록 순서를 변경해 철문→탁본 같은 연쇄 퀘스트 진행 막힘 수정.
- 야영 최초 건설과 증축 이벤트를 `CAMP_FACILITY_BUILT` / `CAMP_FACILITY_UPGRADED`로 분리.
- 일부 시설만 저장된 구세이브에서도 현재 13개 야영 시설을 모두 복원하고, 누락 시설 건설 시 배열에 직접 추가하도록 보강.
- IndexedDB 구세이브의 장비/생활직업/야영지/동료/비행정/시간/퀘스트 및 후발 상태 필드 기본값 마이그레이션 보강.
- 던전 지역 유물편/던전 핵심 전리품을 정식 아이템 DB화하고 던전 획득 `ITEM_GAINED` 연결.
- 제련 산출물/부산물 11종을 정식 아이템 DB에 등록.
- 미리아 전용 퀘스트 `quest_miria_medicinal_herbs` 복구.
- 지하 묘지 `skeleton_warrior` 전투 템플릿 복구해 `wild_wolf` 폴백 제거.
- 연금 작업대 Lv.3의 존재하지 않는 요구 재료를 정식 재료로 교체.
- `wild_herb`를 정식 생활 채집 풀에 연결.
- 전투 패배 후 부활 아이템과 비행정 연료 주입에도 `ITEM_USED` 이벤트 연결.

## 3.2 오류 박멸 최종 정적 검증
- 런타임 데이터 하드 참조 전수검사: 누락 0건.
- 검사 규모: 퀘스트 89 / 인카운터 92 / 캐릭터 51 / 아이템 302 / 장비 424 / 스킬 143 / 레시피 33 / 지역 몬스터 120 / EnemyTemplate 6 / 던전 30.
- 핵심 TypeScript 의존 그래프 별도 컴파일: 내부 오류 0건.
- 핵심 회귀 테스트 및 추가 런타임 검증 전부 PASS.
- 전체 React/Vite/Express 앱 빌드는 작업 환경에 프로젝트 의존성이 설치되지 않아 별도 실제 실행 환경에서 최종 스모크 테스트 필요.
