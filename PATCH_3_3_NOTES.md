# 판타지악 3.3 패치 진행 기록 — 동반자/펫 기반 구조 1차

기준본: `판타지악_3.2_오류박멸_수정본_20260830.zip`

## 이번 반영 범위

### 1. 동반자 상위 분류
- `CompanionKind = HUMANOID | PET` 추가.
- 구 세이브에서 `kind`가 없는 모든 기존 동료는 자동으로 `HUMANOID` 처리.
- PET에는 기존 사람형 동료의 `physicalAge >= 18`, `gender = 남성` 마이그레이션을 강제하지 않도록 분기.

### 2. 신뢰 / 호감 / 유대 분리
- `CompanionBond.affection` 추가.
- 구 세이브 호감도는 연결된 `MajorCharacter.relationship`을 우선 사용하고, 없으면 기존 신뢰도를 폴백으로 사용.
- 신규 `modifyCompanionRelationship()` 추가:
  - `trustDelta`
  - `affectionDelta`
  - `bondExpDelta`
  를 독립 적용.
- 기존 `modifyCompanionBond()`는 3.2 호환 래퍼로 유지하여 기존 호출부 동작을 깨지 않음.
- MajorCharacter의 `trust`와 `relationship`도 각각 동기화.

### 3. 펫 타입/상태 골격
- 펫 분류: `ANIMAL | INSECT`.
- 1차 종족 12종:
  - 동물형 8: 늑대, 여우, 개, 고양이, 토끼, 사슴, 곰, 멧돼지
  - 곤충형 4: 딱정벌레, 사마귀, 나방, 전갈
- 펫 전용 상태:
  - 야생성
  - 친밀도
  - 충성도
  - 성욕 수치
  - 배설 욕구
  - 임계 기록
  - 요청 상태/거부 횟수 골격
  - 개인 플래그

### 4. 펫 사용자 이벤트 슬롯
- 종족별 빈 사용자 작성 슬롯을 추가.
- 성욕 요청 30/50/70/100
- 수락/거절/거부 한계
- 배설 요청 30/50/70/100
- 수락/거절/거부 한계
- 모든 문자열 기본값은 빈 문자열.
- 실제 연출 내용은 코드에 포함하지 않음.

### 5. 동반자 UI 1차
- 기존 동료 화면에 `[동료] [펫]` 탭 추가.
- HUMANOID:
  - 유대 등급 / 신뢰 / 호감 표시.
- PET:
  - 동물형/곤충형
  - 유대 등급 / 친밀 / 충성 / 야생성
  - 펫 전용 성욕 / 배설 욕구 표시.
- 펫에게 인간 종족/성별/직업 정보를 표시하지 않도록 분기.

## 검증
- 핵심 TypeScript 의존 그래프 별도 CommonJS 컴파일 성공.
- 신규 구조 관련 내부 타입 오류 0건.
- 전체 프로젝트 타입체크에서 남는 React/lucide/react-jsx-runtime 오류는 이 작업 환경의 외부 패키지 미설치 문제.
- 런타임 기반 회귀 테스트 7/7 통과.

## 다음 구현 권장 순서
1. 펫 실제 영입 함수 및 생성 팩토리.
2. 펫 욕구 시간/스토리 증가 엔진 연결.
3. 기존 companionNeedQueue를 HUMANOID/PET 공통 요청 큐로 개편.
4. 임계치 즉시 0 초기화 방식 → 요청/수락/거절 상태 머신으로 변경.
5. 거부 스택/쿨다운/중복 요청 방지.
6. 펫 전투 스탯/스킬/성장 트리.
7. 퀘스트 GameEvent (`PET_RECRUIT`, `PET_BOND`, `PET_FAMILIARITY`, `PET_LOYALTY`) 연결.

## 3.3 동반자·펫 2차
- 펫 전용 `recruitPet()` 영입 API 추가. 사람형 동료의 성별/신체나이 정규화를 통과하지 않는다.
- 펫 욕구 진행을 사람형 동료 욕구와 분리했다.
- 스토리 로그 경과: 펫 성욕 증가(종족별 배율 적용).
- 시간 경과: 펫 배설 욕구 증가(종족별 배율 적용).
- 30/50/70/100 임계치를 넘으면 `PetRequestState`를 생성하며 같은 요청은 해결 전 중복 생성하지 않는다.
- 요청 생성 시 욕구를 즉시 0으로 만들지 않는다.
- 수락: 해당 욕구 0, 임계 기록 초기화, 거부 횟수 초기화, 요청 종료.
- 거절: 욕구 유지, 종족별 거부 횟수 증가.
- 거부 한계 도달: 구체 행위를 엔진에서 결정하지 않고 `REFUSAL_LIMIT` 추상 후속 이벤트 큐만 생성한다.
- 펫 종족 정의에 `refusalLimits.desire / bathroom` 추가.
- 동반자 UI에서 활성 펫 요청의 종류/임계/거절 횟수와 수락·거절 버튼을 표시한다.
- `CompanionNeedCue`를 HUMANOID/PET 공용 큐로 확장했다(`entityKind`, `BATHROOM`, `requestId`, `phase`).
- 기존 사람형 동료 욕구 루프는 PET을 건너뛰어 이중 증가를 방지한다.
- 세이브 정규화는 기존 `normalizePetState()`를 통해 요청 상태를 함께 보존한다.

### 2차 검증
- 핵심 TS 그래프 타입체크: 내부 오류 0.
- 런타임 회귀: 펫 영입 / 성욕 요청 / 거절 유지 / 수락 해소 / 배설 요청 생성 통과.
- 전체 UI 타입체크에서 외부 React/Vite/Node 모듈 부재 외 신규 내부 오류 없음.

## 3.3 동반자·펫 3차 — 관계 성장 / 돌봄 / 야생성 / 명령 판정

### 추가
- `PetCareAction`: FEED / PLAY / GROOM / TRAIN.
- `PetActivityState`: 일일 돌봄 반복 횟수, 누적 돌봄 횟수, 명령 성공/실패/독자행동 기록.
- 펫 종족 DB에 `careMultipliers` 추가.
- `performPetCare()`:
  - 친밀도 / 충성도 / 야생성 / 기존 유대 EXP를 실제 변경.
  - 같은 날 같은 돌봄 반복 효율 100% → 65% → 35% → 10%.
  - 날짜가 바뀌면 일일 반복 효율 초기화.
- `getPetCommandRates()`:
  - 충성도·친밀도 상승 시 명령 수행 확률 증가.
  - 야생성 상승 시 명령 수행 확률 감소 및 독자 행동 확률 증가.
  - 확률은 수행 / 독자 행동 / 실패의 합이 100%가 되도록 계산.
- `resolvePetCommand()` 및 엔진 공개 래퍼 `issuePetCommand()` 추가.
- `careForPet()`, `getPetBehaviorRates()` 공개 API 추가.
- 동반자 UI 펫 탭에 돌봄 4종, 유대 EXP, 명령 확률, 명령 시험 버튼 추가.
- `normalizePetState()`에서 2차 이전 펫 세이브에 `activity`를 자동 복구.

### 현재 범위
- FEED는 3차에서 관계 성장용 '먹이 주기 행동'으로 구현되어 있으며 실제 인벤토리 먹이 아이템 소비는 아직 연결하지 않음.
- 명령 판정 엔진/통계는 구현했지만 실제 전투 AI 행동 교체는 후속 패치 대상.

### 검증
- 핵심 TypeScript 의존 그래프 컴파일: 내부 오류 0.
- 런타임 회귀 테스트 6/6 통과:
  1. 훈련으로 충성도 상승 / 야생성 감소 / 유대 EXP 증가.
  2. 같은 날 반복 효율 감소.
  3. 다음 날 반복 효율 초기화.
  4. 친밀·충성·야생성에 따른 명령 수행 확률 차이.
  5. 친밀·충성·야생성에 따른 독자 행동 확률 차이.
  6. 명령 결과 누적 통계 저장.
- 전체 React UI 타입 검사는 현재 실행 환경에 React/motion 및 이미지 모듈이 없어 외부 모듈 오류가 발생하지만, 이번 수정에서 추가된 별도 내부 타입 오류는 확인되지 않음.

## 3.3-3A 길들이기 통합 수정

- 기존 펫 돌봄의 `훈련하기` UI 명칭을 `길들이기`로 변경.
- 구세이브 호환을 위해 내부 `PetCareAction` 키 `TRAIN`은 유지.
- 길들이기는 기존 훈련 효과를 유지:
  - 친밀도 +2
  - 충성도 +5
  - 야생성 -4
  - 유대 EXP +4
- 길들이기에 선제 욕구 완화 효과 통합:
  - 성욕 -12
  - 배설 욕구 -10
- 같은 날 반복 시 기존 돌봄 효율 감소(100% → 65% → 35% → 10%)가 관계 변화와 욕구 완화에 함께 적용.
- 길들이기 결과 현재 활성 요청의 욕구가 해당 임계치 아래로 내려가면 요청 상태를 자동 해제하고 거부 횟수를 0으로 초기화.
- 자동 해제된 요청의 `requestId`와 연결된 대기 `companionNeedQueue` 항목도 제거하여 오래된 요청이 Gemini 로그에 뒤늦게 출력되는 문제 방지.
- 동반자 UI의 `길들이기` 버튼을 연분홍색으로 변경.
- 펫 상세 섹션 제목을 `펫 유대 · 길들이기`로 변경.

## 3.3 4차 — 실제 먹이 / 전투 펫 행동 연결
- 펫 12종에 `preferredFoodIds`, `likedFoodIds` 추가.
- 기존 생활재료 DB의 실제 식재료를 펫 먹이로 재사용한다.
- 동반자 UI에서 현재 인벤토리에 실제 존재하는 선호 먹이만 표시한다.
- 먹이 급여 시 해당 아이템을 정확히 1개 소비한다.
- 최애 먹이는 기본 `FEED` 돌봄 효과에 친밀도 +2 / 충성도 +1 추가 보너스를 준다.
- 기존 일반 `먹이 주기` 버튼은 제거하고 인벤토리 먹이 선택식 UI로 교체했다.
- 펫의 `명령 수행 / 독자 행동 / 명령 실패` 확률을 전투 AI의 실제 행동 계획에 연결했다.
  - 명령 수행: 기존 설정 전술대로 행동.
  - 독자 행동: 해당 행동에 한해 공격적 전술로 독립 판단.
  - 명령 실패: 방어 태세를 선택.
- 전투 로그에 펫 행동 판정 결과를 별도 기록한다.
- 핵심 TypeScript 의존 그래프 컴파일 오류 0건 확인.
- 실제 급여 소비와 전투 실패 행동 전환 회귀 테스트 완료.

## 3.3 펫 연출 참조 슬롯/UI 표시명 보강
- 펫 종족별 사용자 작성 참조 슬롯을 배열형으로 확장했다.
  - 성욕 요청 참조 6칸
  - 배설 욕구 요청 참조 6칸
  - 성욕/배설 요청 수락 반응 각 3칸
  - 성욕/배설 요청 거부 반응 각 3칸
  - 반복 거부 한계의 강제 요청 단계 참조 각 3칸
- 모든 참조 슬롯 기본값은 빈 문자열이며, 사용자 작성 전에는 어떤 연출 문자열도 포함하지 않는다.
- 펫 상세 헤더는 내부 `speciesId`/`category` 문자열을 직접 노출하지 않고 종족 DB `displayName`과 `동물형/곤충형` 한글 라벨만 사용하도록 확인/보강했다.

## 3.3 펫 종별 연출 참조 완전 분리
- `src/data/pets/petEventReferences.ts` 신규 추가.
- 늑대/여우/개/고양이/토끼/사슴/곰/멧돼지/딱정벌레/사마귀/나방/전갈 12종이 각각 완전히 독립된 사용자 참조 슬롯 객체를 가진다.
- 각 종은 독립적으로 다음 슬롯을 보유한다.
  - 욕구 요청 6칸
  - 배설 욕구 요청 6칸
  - 요청 수락 반응 각 3칸
  - 요청 거부 반응 각 3칸
  - 반복 거부 한계 후속 요청 각 3칸
- `getPetUserReferencePool(speciesId, need, phase)` 추가: 반드시 해당 `speciesId`의 참조만 반환하며 타 종족 참조와 섞이지 않는다.
- 비어 있거나 `TODO_USER*` / `USER_TODO`인 슬롯은 사용 가능한 참조로 취급하지 않는다.
- 기존 `petDatabase.ts`의 공통 빈 슬롯 생성기를 제거하고 종족별 참조 레지스트리를 직접 연결했다.
- 타입체크 성공. 런타임 독립성 검증에서 늑대 슬롯 수정 후 여우 참조 풀이 비어 있음을 확인했으며 12종 레지스트리 모두 존재함을 확인했다.

## 2026-08-31 — 펫 목록 확정 / 펫 장착 칸

- 펫 목록을 9종으로 재정의.
  - 동물형: 늑대, 개, 검치호랑이, 곰, 멧돼지
  - 곤충형: 챠루스, 챠루스 리퍼, 나방, 음요충
- 기존 임시 펫 종(여우/고양이/토끼/사슴/딱정벌레/사마귀/전갈)은 신규 획득 DB에서 제거.
- 구 세이브의 제거된 임시 종은 가장 가까운 현행 종으로 안전 마이그레이션하여 로드 크래시를 방지.
- 플레이어 상태에 `equippedPetId` 전용 펫 장착 칸 추가.
- 동반자 UI 펫 상세에 `펫 장착 칸 / 이 펫 장착 / 장착 해제` 추가.
- 펫 장착은 전투 파티 편성과 별개. 이후 탈것/탐험/펫 패시브에서 공통 참조 가능.
- 펫 획득 함수는 `PetAcquisitionSource = SHOP | SPECIAL_ENCOUNTER`를 필수로 받아 일반 영입과 분리.
- 연출 문자열 내용은 수정하지 않음. 현재 9종용 독립 빈 참조 슬롯 구조만 타입 호환을 위해 유지.
- 펫 핵심 타입/DB/마이그레이션 별도 TypeScript 컴파일 오류 0건.

- 펫 내부 ID도 `YOEUM_BUG`에서 `EUMYO_BUG`로 전면 변경. 구세이브 호환성은 고려하지 않음.

## 2026-08-31 — 펫 시스템 정리 / 성장·등급·연출 로그 통합

- 펫 등급 5단계 실제 구현: 일반 / 고급 / 희귀 / 영웅 / 전설.
- 등급별 성장 레벨 상한: 10 / 20 / 30 / 40 / 50.
- 전투 승리 EXP의 75%를 활성 전투 펫에게 지급하며, 레벨 상한 도달 시 다음 등급으로 자동 승급.
- 등급 상승 시 배설 욕구 증가속도는 유지하고, 성욕 증가 배율과 야생성 증가 배율만 크게 상승.
- 성장 레벨이 높아질수록 스토리 진행 시 야생성 자연 증가폭이 추가 상승.
- 『신진대사 강화』 Lv.0~5 구현: 레벨당 기본 스탯 +3%, 배설 욕구 증가속도 +20%. 등급 배율과 분리.
- `PetCareAction`을 PLAY / GROOM / TAME으로 정리하고 구 `TRAIN` 키 제거.
- 아이템 소비 없는 일반 FEED 돌봄 경로 제거. 먹이 효과는 실제 인벤토리 아이템을 소비하는 `feedPetItem()`만 사용.
- 길들이기와 펫 욕구 요청 수락/거절/거절 한계는 팝업·토스트가 아니라 실제 본문 게임 로그를 생성.
- 펫 상호작용은 예정 결과 계산 → 전용 Gemini 로그 생성 → 성공 검증 → 상태/로그 1회 커밋 순서로 처리. 생성 실패 시 상태 미적용.
- 펫 상호작용 로그 생성 중 자유 행동 입력과 관련 버튼을 잠가 stale-state 이중 커밋 방지.
- 펫 종별 연출 참조는 현재 확정 9종의 speciesId에 따라 독립 조회.
- Gemini 상태 요약은 펫의 `petState`에서 친밀도/충성도/야생성/성욕/배설 욕구를 직접 읽으며 펫을 인간형 성별로 폴백하지 않음.
- 거절 한계 후속 로그가 성공하면 해당 욕구 사이클을 종료하고 욕구/임계 기록/거부 스택을 초기화하여 다음 30 임계부터 정상 재시작.
- 실제 전투와 상태 테스트가 동일한 `rollPetCommandOutcome()` 판정 함수를 공유하도록 통합.
- 실제 전투 명령 결과는 CombatScreen → App → `recordPetCommandOutcome()` 경로로 통계에 누적.
- 구 펫 종 ID 마이그레이션 제거. 현행 9종 ID 외 값은 펫 상태로 정규화하지 않음.
- 펫 상호작용 로그는 비노골적인 상태·몸짓·돌봄 묘사 범위로 생성하도록 프롬프트 제한.

검증:
- 펫 핵심 + battleEngine 대상 TypeScript 컴파일 오류 0건.
- 런타임 회귀 테스트 7/7 통과.
- 전체 `tsc --noEmit`의 122건은 현재 실행 환경에 미설치된 React/Vite/Express/Node/Google GenAI 모듈 관련 오류뿐이며 신규 내부 타입 오류 0건.

## 3.3 펫 시스템 정리 패치 — 2026-08-31

### 성장 / 등급
- 펫 등급 5단계: 일반 / 고급 / 희귀 / 영웅 / 전설.
- 등급별 레벨 상한: 10 / 20 / 30 / 40 / 50.
- 전투 승리 EXP의 75%를 전투 참가 펫에게 지급하며 레벨 상한 도달 시 다음 등급으로 자동 승급.
- 등급 상승에 따라 성욕 증가 배율과 야생성 증가 배율이 크게 상승.
- 배설 욕구 증가속도는 등급 영향을 받지 않으며 『신진대사 강화』 퍽만 추가 배율을 제공.
- 『신진대사 강화』 최대 Lv.5: 레벨당 기본 스탯 +3%, 배설 욕구 증가속도 +20%.

### 욕구 요청 상태 머신
- 30 / 50 / 70 / 100 임계 요청 유지.
- 반복 거절 한계로 요청이 닫힌 뒤 욕구가 이미 높은 상태여도, 다음 진행 틱에서 아직 처리하지 않은 임계치를 순서대로 재개하도록 수정.
- 수락/거절 응답 시 동일 requestId의 기존 REQUEST 큐를 제거해 중복 연출 방지.
- 길들이기로 현재 임계 아래까지 욕구가 낮아지면 활성 요청과 대기 큐를 함께 해제.

### 연출 로그 / 원자적 커밋
- 길들이기 및 펫 욕구 요청 응답은 알림창/토스트가 아니라 본문 게임 로그를 생성.
- 종별 사용자 참조가 있으면 해당 펫 종족 + 욕구 종류 + 단계에 맞는 참조 풀만 사용.
- 로그 생성 성공 전에는 상태를 커밋하지 않음. 성공한 로그 하나와 상태 변경을 함께 1회 커밋.
- 로그 생성 실패 시 행동 자체를 적용하지 않음.
- 동기 ref 잠금을 추가해 React 상태 갱신 전의 초단기 더블클릭도 차단.
- 로그 생성 중 일반 행동 입력과 펫 상태 변경 버튼(길들이기/돌봄/먹이/퍽/장착/요청 응답)을 잠가 오래된 상태 덮어쓰기 방지.
- 펫 본능 욕구 장면은 서버 지침상 비성적·비노골적 행동/분위기 수준으로 제한.

### 전투 / UI 정리
- 실전 전투의 펫 명령 판정 결과는 CombatScreen → App → recordPetCommandOutcome 경로로 실제 통계에 누적.
- 펫 UI는 등급을 한국어 표시명으로만 노출하며 내부 enum/종족 ID를 직접 표시하지 않음.
- 구식 TRAIN 키는 TAME으로 교체 완료.
- 일반 돌봄에서 아이템을 소비하지 않는 FEED 우회 경로 제거. 실제 먹이는 feedPetItem()만 사용.

### 검증
- 펫 핵심 TypeScript 모듈 별도 타입체크 오류 0건.
- 회귀 테스트 8/8 통과:
  1. 일반→고급 자동 승급
  2. 등급별 성욕 증가 배율
  3. 등급별 야생성 증가 배율
  4. 신진대사 강화 Lv.5의 배설 속도 ×2
  5. 고수치 상태에서 미처리 임계 요청 재개
  6. 길들이기 활성 요청 해제
  7. 길들이기 대기 큐 해제
  8. 실전 명령 결과 통계 기록


## 2026-08-31 — 펫 오류 박멸 / 장착 전용 상호작용 / 검증 모드

- 펫 돌봄·먹이·길들이기·신진대사 강화·욕구 요청 응답을 현재 `equippedPetId`와 일치하는 펫에만 허용하도록 엔진/App/UI 3중 검증 추가.
- 미장착 펫은 스토리/시간 경과에 따른 펫 전용 성욕·배설 욕구·야생성 자연 진행과 요청 생성에서도 제외.
- 길들이기 기본 효과 유지: 성욕 -12, 배설 욕구 -10, 야생성 감소(종 분류/반복 효율 적용).
- 반복 거절 한계 `REFUSAL_LIMIT` 발생 시 해당 욕구 사이클을 종료하는 동시에 야생성이 크게 증가하도록 수정. 등급 야생성 배율의 제곱근을 적용하여 일반 약 +10, 전설 약 +16.43 범위로 설정.
- 펫 영입 특수 인카운터 사용자 참조를 종별 2칸씩 추가. 빈 문자열/플레이스홀더는 제외하며 두 칸 모두 비어 있으면 해당 종은 영입 인카운터 후보에 들어가지 않고 `rollPetAcquisitionEncounterReference()`가 null을 반환.
- 검증용 임시 `PET_TEST_UNLOCK_ALL = true`: 새 게임 및 상태 정규화 시 현재 9종 펫을 모두 보유 목록에 주입. 정식 `CHARACTER_RECRUITED` 이벤트를 발생시키지 않아 퀘스트 진행을 오염시키지 않음.
- 테스트 펫은 기본적으로 전투 파티 비활성, 펫 장착 칸도 비어 있는 상태로 시작하여 원하는 종을 직접 장착해 검증 가능.
- UI에서 미장착 펫의 돌봄/길들이기/먹이/신진대사 강화 버튼 비활성화 및 요청 응답 패널 숨김.

밸런스 검증:
- 일반 등급 성욕 증가: 종별 약 2.7~3.75 / 스토리 로그. 길들이기 1회 -12로 약 3~4회 로그분 완화.
- 전설 음요충 성욕 증가: +12 / 스토리 로그. 고등급에서는 길들이기 한 번으로 한 로그 정도만 상쇄되어 관리 부담이 크게 증가.
- 배설 욕구: 나방 기준 약 +1.68/시간, 신진대사 강화 Lv.5 약 +3.36/시간(정확히 ×2). 등급 영향 없음.
- 강제형 요구 야생성 증가: 일반 +10, 전설 +16.43. 1회 길들이기 야생성 감소(-3~-4)보다 훨씬 크지만 즉시 100 고정은 피함.
- 회귀 테스트 통과: 9종 테스트 해금, 미장착 상호작용/욕구 진행 차단, 길들이기 3종 감소, 강제 요구 야생성 상승, 빈 영입 인카운터 미발생, 전설 증가폭, 신진대사 ×2.
- `recruitPet()` 런타임에서도 획득 출처를 재검증하여 `SHOP`/`SPECIAL_ENCOUNTER` 이외의 직접 호출을 거부.

## 2026-08-31 — 펫 사용자 연출 완전 분리
- 펫 사용자 작성 연출 문자열을 본체 `src/data/pets/petEventReferences.ts`에서 완전히 제거.
- 사용자 전용 파일 `src/user_content/petReferences.ts` 신규 추가.
- 본체는 사용자 전용 파일을 읽고, 빈 문자열 / `USER_TODO` / `TODO_USER...` 값은 자동으로 무시한다.
- 펫 9종의 영입(2), 길들이기(3), 성욕/배설 요청(각 6), 수락(각 3), 거부(각 3), 반복 거부 한계/강제형 요구(각 3) 참조를 종별 독립 유지.
- 길들이기 로그도 이제 해당 펫 종족의 `tameReferences`를 참조할 수 있다.
- 영입 참조 2칸이 모두 비어 있으면 해당 펫 영입 인카운터는 계속 미발생 처리된다.
- 이후 본체 패치 시 `src/user_content/petReferences.ts`만 보존하면 사용자 작성 펫 연출을 유지할 수 있다.
