# 판타지악 3.2 오류 박멸 진행 기록

## 현재 상태
- 2026-08-30 중간본을 기준으로 오류 박멸 전수 패스를 이어서 진행했다.
- 퀘스트/이벤트, 보상, 저장 마이그레이션, 던전 보상, 제련 재료, 야영지, 전투 종료, 월드 이동, 비행정, Gemini 재시도 커밋 경로를 재점검했다.
- 현재 정식 데이터의 하드 참조 전수검사 결과: **누락 0건**.
- 핵심 TypeScript 런타임 의존 그래프 별도 컴파일: **내부 오류 0건**.
- 전체 React/Vite/Express 빌드는 작업 컨테이너에 프로젝트 의존성이 설치되지 않아 최종 실행 검증하지 못했다.

## 이번 이어서 점검에서 추가 수정한 오류

### 1. 퀘스트 동일 이벤트 중복 소비 방지
- 한 번의 외부 이벤트가 현재 단계 완료 후 다음 단계의 동일 이벤트형 목표까지 재사용되던 문제 수정.
- 예: 같은 NPC 대화가 연속 두 TALK_NPC 단계를 한 번에 완료하지 않도록 변경.
- 단계 전환 뒤에는 보유 아이템/생활직업 레벨/야영 시설/동료 유대 등 **상태 기반 목표만** 연쇄 재평가한다.

### 2. 퀘스트 보상 EXP 레벨업 누락 수정
- 퀘스트 보상 EXP가 `experience`에만 더해지고 레벨업 계산을 통과하지 않던 문제 수정.
- 다중 레벨업, 스탯 포인트, 재능 포인트, 레벨 성장 HP/MP, 패시브 해방석, Lv.5/Lv.20 성장 마일스톤을 정상 적용한다.

### 3. 퀘스트 보상 후속 이벤트 연결
- 퀘스트 보상으로 직접 지급되는 아이템/장비/열쇠가 `ITEM_GAINED`를 발생시키도록 보강.
- 생활직업 보상 EXP로 실제 레벨이 오른 횟수만큼 `PROFESSION_LEVEL_UP` 발생.
- 동료 신뢰 보상은 `COMPANION_BOND_CHANGED`로 연결.
- 보상으로 다른 퀘스트 목표가 충족되는 연쇄 진행을 복구했다.

### 4. 잠금 해제 → 보상 순서 오류 수정
- 잠금 보상을 먼저 지급한 뒤 `LOCK_UNLOCKED`를 발생시키던 순서를 반대로 수정.
- `crypt_iron_gate`처럼 "잠금 해제 후 보상 아이템 획득"이 다음 단계 목표인 퀘스트가 정상 진행된다.

### 5. 야영 시설 건설/증축 이벤트 구분
- 모든 시설 레벨 상승이 `CAMP_FACILITY_BUILT`로 기록되던 문제 수정.
- Lv.0→1은 `CAMP_FACILITY_BUILT`, 기존 시설의 레벨 상승은 `CAMP_FACILITY_UPGRADED`로 분리.

### 6. 부분 구세이브 야영 시설 유실 수정
- 구버전 세이브에 일부 시설만 저장되어 있으면 신규 시설이 영구 누락될 수 있던 문제 수정.
- 저장 정규화 시 현재 `CAMP_FACILITIES_DATABASE`의 모든 정식 시설을 병합한다.
- 정규화를 우회한 상태에서도 누락 시설을 처음 건설하면 시설 배열에 실제로 추가하도록 이중 방어.

### 7. 구세이브 필수 상태 기본값 보강
- `equipment`, `professions`, `campProgress`, `companions`, `airship`, `unlockedLocks` 등 신규 필드가 없는 IndexedDB 구세이브를 안전하게 정규화.
- 시간/퀘스트/동료 장비·유대/성인 상태 보조 배열/페로몬/방광/기생체·알/대화 카운트 등 후발 필드도 기본값 보장.
- 장비 13슬롯, 생활직업 6종, 야영 시설 13종을 현재 구조로 복원한다.

### 8. 던전 보상 유령 아이템 제거
- `dungeon_relic_*`, `dungeon_core_*` 런타임 생성 보상이 정식 아이템 DB에 없어 유령 스택이 되던 문제 수정.
- 지역 유물편/던전 핵심 전리품을 정식 아이템 DB에 등록.
- 동일 지역의 여러 던전이 동일 ID에 서로 다른 표시명을 넣던 충돌도 제거.
- 던전 보상 획득 시 `ITEM_GAINED` 이벤트도 발생한다.

### 9. 제련 산출물/부산물 메타데이터 복구
정식 아이템 DB에 없던 다음 산출물/부산물을 등록했다.
- 동/철/청동/강철/미스릴/천철 주괴
- 광질 슬래그 가루
- 고열 재 파편
- 은 분말
- 빛나는 미스릴 미분
- 천경 파편

### 10. 끊긴 퀘스트/전투 템플릿 복구
- 미리아의 `quest_miria_medicinal_herbs` 복구.
- 지하 묘지 전투가 존재하지 않는 템플릿 때문에 `wild_wolf`로 폴백하던 문제를 막기 위해 `skeleton_warrior` 정식 템플릿 추가.

### 11. 야영 연금 작업대 Lv.3 진행 불능 비용 수정
- 정식 아이템 DB에 존재하지 않는 `영혼석 조각` 요구를 실제 획득 가능한 `빛나는 마나석 파편`으로 교체.

### 12. 초반 메인 퀘스트 약초 획득 경로 복구
- `wild_herb`를 그란디아/포레진의 정식 생활 채집 풀에 연결.
- 초반 메인 퀘스트가 정상 월드 행동으로 진행 가능하도록 보강.

### 13. 패배 후 부활 아이템 이벤트 누락 수정
- 전투 밖 패배 화면에서 부활 아이템을 사용할 때도 `ITEM_USED`가 발생하도록 연결.

### 14. 비행정 연료 아이템 사용 이벤트 누락 수정
- 연료 주입 시 실제 소비 수량/아이템명을 반환하고 `ITEM_USED`를 발생시키도록 연결.
- 연료전지 사용형 퀘스트/로그가 생겨도 중앙 이벤트와 불일치하지 않는다.

## 전수 참조 검사 결과
현재 런타임 DB를 실제 JavaScript 객체로 컴파일한 뒤 다음 참조를 교차검증했다.
- 퀘스트 89
- 인카운터 92
- 주요 캐릭터 51
- 아이템 302
- 장비 424
- 스킬 143
- 레시피 33
- 생활직업 6
- 잠금 5
- 지역 몬스터 120
- 전투 EnemyTemplate 6
- 던전 30
- 몬스터 전리품 프로필 120
- 월드 자원 노드 11
- 제련 레시피 7

검사 대상에는 퀘스트 목표/보상/조건, 캐릭터 customQuestId, 클래스 스킬, 몬스터 스킬/재료, 장비 스킬, 레시피 입출력, 잠금 열쇠, 던전 보상, 야영 업그레이드 재료, 역참, 자원 노드, 제련, 비행정 재료가 포함된다.

**결과: 하드 참조 누락 0건.**

## 회귀 테스트 결과
- 같은 이벤트가 다음 퀘스트 단계에 재사용되지 않음: PASS
- 퀘스트 보상 EXP 레벨업/성장 마일스톤: PASS
- 철문 해제 → 탁본 획득 → 다음 단계 진행: PASS
- 야영 Lv.1→2 증축 이벤트 구분: PASS
- `wild_herb` 정식 채집: PASS
- 던전 런타임 보상 ID 정식 DB 조회: PASS
- 부분 구세이브의 누락 야영 시설 전체 복구: PASS
- 배열에 없는 야영 시설 직접 건설 시 실제 추가: PASS
- 제련 산출물/부산물 메타데이터 조회: PASS
- 미리아 퀘스트 존재: PASS
- 묘지 해골 전투 템플릿 존재 및 늑대 폴백 제거: PASS
- 모든 야영 시설 업그레이드 재료 조회: PASS
- 실제 철 제련 실행 및 정식 주괴 메타데이터 적용: PASS
- 비행정 연료 소비 수량/아이템 메타데이터 반환: PASS

## 추가 확인
- 전투 승리/도주/패배 보상은 종료 가드로 중복 커밋되지 않는 구조를 유지한다.
- 비행정 건조/업그레이드는 UI뿐 아니라 시스템 함수 내부에서 재료·레벨·중복 적용을 검증한다.
- 비행정 여행 연료는 실제 Hex 진행 완료 시 소비되고 `AIRSHIP_TRAVELED`가 발생한다.
- Gemini 서사 거부/무효 응답 재시도는 서버에서 게임 상태를 변경하지 않으며, 클라이언트는 성공 응답 하나를 받은 뒤에만 상태를 커밋한다. 패치 12의 "동일 행동 중복 적용 금지" 원칙을 유지한다.

## 남은 검증 한계
작업 컨테이너에는 프로젝트 `node_modules`가 없고 의존성 설치 시도가 완료되지 않아 React/Vite/Express/@google/genai까지 포함한 **전체 앱 빌드 및 브라우저 실제 플레이 스모크 테스트**는 이 환경에서 완료하지 못했다.

대신 외부 UI 패키지와 무관한 핵심 TypeScript 의존 그래프는 별도 컴파일했으며 내부 오류 0건, 위 회귀/참조 검사를 통과했다.
